name: 📚 Renovação Automática de Livros

on:
  workflow_dispatch:
  schedule:
    - cron: '0 15 * * *'

jobs:
  renovar-livros:
    runs-on: ubuntu-latest

    steps:
      - name:  checkout
        uses: actions/checkout@v4

      - name: 🏗️ Configurar Python 3.10
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      # NOVO: Instala o Node.js para podermos usar o 'npx'
      - name: 🏗️ Configurar Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20' # Versão LTS estável

      - name: 📦 Instalar dependências do Python
        run: |
          python -m pip install --upgrade pip
          pip install selenium python-dotenv emoji

      # CORREÇÃO: Nova forma de instalar o Chrome
      - name: 📦 Instalar Google Chrome (via Playwright)
        run: |
          # Instala o pacote 'playwright' do node
          npm install playwright 
          # Roda o comando recomendado para instalar o Chrome e suas dependências
          npx playwright install --with-deps chromium

      - name: 🚀 Executar script de renovação
        env:
          UCB_EMAIL: ${{ secrets.UCB_EMAIL }}
          UCB_PASS: ${{ secrets.UCB_PASS }}
        run: python Biblioteca.py

      - name: 💾 Carregar Artefatos de Debug
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: debug-artifacts
          path: |
            *.png
            *.html