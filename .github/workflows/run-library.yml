name: 📚 Renovação Automática de Livros

on:
  workflow_dispatch: # Permite rodar manualmente pela aba "Actions"
  schedule:
    - cron: '0 15 * * *'

jobs:
  renovar-livros:
    runs-on: ubuntu-latest

    steps:
      - name:  checkout
        uses: actions/checkout@v4

      # 1. Iniciar Display Virtual (XVFB)
      - name: Instalar e Iniciar XVFB (Display Virtual)
        uses: coactions/setup-xvfb@v1

      - name: 🏗️ Configurar Python 3.10
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: 📦 Instalar dependências do Python
        run: |
          python -m pip install --upgrade pip
          pip install selenium python-dotenv emoji

      # 2. Configuração de Navegador Robusta (Playwright)
      # CORREÇÃO AQUI: Tag alterada para @v1
      - name: Instalar Google Chrome (via Playwright)
        uses: microsoft/playwright-github-action@v1 # <-- CORREÇÃO
        with:
          browser: chromium

      - name: 🚀 Executar script de renovação
        env:
          UCB_EMAIL: ${{ secrets.UCB_EMAIL }}
          UCB_PASS: ${{ secrets.UCB_PASS }}
        run: python Biblioteca.py